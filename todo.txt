bugs...
  vue screwed up: Not on a function.

  added types to js file

  should have better explantion: Cannot process: found call resolved to a different symbol in c:/Users/mark/apps/test2/out/extension.js

todo ...
  check mixing js and ts
  add detailed explanations to the dialog
  test arrow-functions

params
  default
  rest
  deconstructing
  Optional parameters (TypeScript): fn(x?: number) 
  Parameter properties (TypeScript): constructor(public x: number).
  Parameter decorators (TypeScript, experimental): @dec param for metadata/transform
  earlier params can be used by later defaults only if defined.
  Function arity (function.length): counts parameters before the first default/rest
  Overloads (TypeScript)
  Binding/call/apply effects: call/apply can change this and pass args dynamically.
  Variants inside destructuring: nested defaults, rest within destructuring (function f({a, ...rest} = {})).


